<?php
session_start();require_once '../shared/config.php';require_once '../shared/security.php';o2oCsrfToken();$db=getDB();$token=trim($_GET['token']??'');$message='';$error='';
if($token!==''){
 $hash=hash('sha256',$token);$st=$db->prepare("SELECT id FROM customers WHERE verification_token_hash=? AND verification_expires_at>NOW() AND email_verified_at IS NULL LIMIT 1");$st->execute([$hash]);$customer=$st->fetch();
 if($customer){$db->prepare("UPDATE customers SET email_verified_at=NOW(),verification_token_hash=NULL,verification_expires_at=NULL,failed_login_count=0,locked_until=NULL WHERE id=?")->execute([(int)$customer['id']]);$message='Your email address is verified. You can now sign in.';}else{$error='This verification link is invalid, expired, or already used.';}
}else{$error='Verification link is missing.';}
?><!doctype html><html><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Verify Email – O2O Tradition</title><style>body{font-family:Arial;background:#FAF6EE;color:#3D2B0F}.card{max-width:520px;margin:80px auto;background:#fff;border:1px solid #E8E0D0;padding:30px}.ok{background:#ECFDF5;color:#065F46;padding:12px}.err{background:#FEF2F2;color:#991B1B;padding:12px}.btn{display:inline-block;margin-top:18px;background:#1A1108;color:#C9A84C;padding:12px 18px;text-decoration:none}</style></head><body><main class="card"><h1>O2O Tradition</h1><?php if($message):?><div class="ok"><?=htmlspecialchars($message)?></div><?php else:?><div class="err"><?=htmlspecialchars($error)?></div><?php endif;?><a class="btn" href="login.php">Go to Login</a></main></body></html>